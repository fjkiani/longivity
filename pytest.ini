[pytest]
testpaths = tests
addopts = -v --tb=short
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Repo root on sys.path so agents/ is importable as both 'agents' and 'longivity.agents'
pythonpath = .
